# Frontend Environment Variables (for Vercel deployment)
# Leave blank if deploying backend on same domain or set to your backend URL

# Backend API URL (used by frontend)
# Leave empty for: 
#   - Local dev (falls back to http://localhost:4000/api)
#   - Same-domain deployment (uses /api relative path)
# Set to deployed backend URL when backend is on separate domain:
REACT_APP_API_URL=https://your-backend-domain.onrender.com/api

# Or use underscore version (alternative env var name):
API_URL=https://your-backend-domain.onrender.com/api

# Example values:
# - Local: http://localhost:4000/api
# - Render backend: https://your-app-backend.onrender.com/api
# - Railway backend: https://your-app-backend-on-railway.up.railway.app/api
# - Same domain: /api (relative)
